# CX-001: hist12 OOF stacker consumer

## Task ticket

- Task ID: CX-001
- Base SHA: `35fdb15107b09ba31b54c04234da157def722db5`
- Owner: Codex
- Goal: Validate and consume hist12 encoder group-OOF artifacts, align them with legacy baseline OOF probabilities, and evaluate/train a leakage-aware meta-classifier.
- Owned files: `scripts/stacker_h12/**`, `tests/test_stacker_h12.py`, this handoff.
- Forbidden files: main worktree, `submit/**`, active `colab/**`, canonical `context/**` outside `context/handoffs/codex/**`.
- Inputs and SHA256: production hist12 OOF artifacts are not available yet (`UNKNOWN`). Expected contract is `oof_fold{k}.npz` with `ids`, `probs`, `y_true`, `actions`, `fold`, plus `fold_map.csv`; legacy OOF contains `row_ids.json`, `classes.json`, `y_true.json`, and named `*_probs.npy` components.
- Validation / gate: synthetic contract tests; probability, ID, label, action-order, fold-map, and session-group integrity checks; deterministic grouped meta-OOF evaluation; independent reviewer/tester required before main promotion.
- Definition of Done: isolated CLI/library implementation, focused tests passing, no production-score claim, commit on `codex/context-v2`, handoff completed with exact commands and limitations.
- Next owner: Claude reviewer/tester, then Claude control owner for optional cherry-pick and real OOF execution.

## Handoff

- Task ID: CX-001
- Branch / commit: codex/context-v2 / 8b0a2530bfa890727571437f42a8d17bca275825
- Files changed: scripts/stacker_h12/{__init__,common,features,train}.py, tests/test_stacker_h12.py, this handoff.
- Inputs and artifact hashes: production hist12 OOF remains UNKNOWN and was not executed. The structured feature contract is pinned to submit_candidates/alpha09/src/sprint080_features.py at commit 4400df449a672aceffbe15eac0773e4a76c46a3a, canonical content SHA256 356cff34fe6bd658938a49c5bdd5e5ea64f13e0973f94d95621db116d0bf81c3.
- Implementation hashes:
  - common.py: 02DE9983749DFD7C3CB0B8BD86DA891DE11677739F3F07A2FD3F33A29184B98E
  - features.py: 23C69CC05D70CD97D16EAEC08697F521F6D08003D06070F86841935A3B02F470
  - train.py: 801463B2840E0D6B4E31B5AACFBE97FEE862283899C21EC5654CA50ED7A97EC1
  - test_stacker_h12.py: 68C28FF29F1E0B3A737165AD4CD4E0A2D3672333C2C580542442DC56FF3D7CB9
- Validation results:
  - Focused pytest: 16 passed (four upstream joblib/NumPy deprecation warnings).
  - py_compile: PASS.
  - python -m scripts.stacker_h12.train --help: PASS.
  - staged git diff --check: PASS.
  - Independent tester: PASS, including validate-only and full synthetic 5-fold CLI probes.
  - Independent reviewer: initial FAIL on fail-open parity and record/session alignment; fixes applied; final re-review PASS.
- Known limitations:
  - diagnostic_meta_cv is meta-layer cross-fitting over fixed base OOF, not end-to-end nested CV. Every manifest sets promotion_eligible=false; final all-OOF fit is never scored in sample.
  - baseline_origin is caller-declared. Until an alpha09 sparse-OOF manifest/hash is supplied and verified, teammate_baseline_parity_verified=false and teammate_parity=false, even when the caller claims alpha09_sparse_oof.
  - An NPZ-internal `id`↔`probs` row-order mismatch cannot be detected structurally. Mitigation: the generator saves IDs and probabilities during the same validation-row traversal, and the handoff locks every generated NPZ with SHA256.
  - The real five hist12 OOF NPZ files and SHA256 manifest are not yet available, so no model artifact or score was produced.
  - The sparse Codex worktree excludes submit/** and removed submit_candidates/**. The full suite therefore had 17 passes and 19 setup errors from those missing paths; rerun the full suite after cherry-pick in main.
  - Terra routing was attempted but blocked by account usage limits; implementation used the current Codex session and was independently reviewed/tested.
- Recommended decision: keep as infrastructure-only PASS. Do not log a score experiment or promote a model until real OOF validation passes and a frozen shadow set or end-to-end outer evaluation is available.
- Required reviewer/tester checks:
  - Claude reviewer inspects commit 8b0a253 and confirms the pinned feature source against commit 4400df4.
  - Claude tester runs focused tests and the full main-worktree suite after cherry-pick.
  - Validate all five real oof_fold*.npz, fold_map.csv, and run metadata hashes before fitting.
  - Supply verified alpha09 sparse baseline OOF; using legacy linear_probs.npy is explicitly a non-parity proxy.
  - Any score or submission decision requires a separate session-group shadow/nested evaluation and Claude control-owner approval.

## Resume commands

~~~powershell
cd C:\dev\codex-context-v2
C:\dev\2026-AI-DACON\.venv\Scripts\python.exe -m pytest -q -p no:cacheprovider tests\test_stacker_h12.py
C:\dev\2026-AI-DACON\.venv\Scripts\python.exe -m scripts.stacker_h12.train --help
git show --stat 8b0a253
~~~

After Claude provides the real OOF directory, first run the CLI with --validate-only. Use --baseline-origin legacy_linear_proxy only for an explicitly non-parity plumbing check.
